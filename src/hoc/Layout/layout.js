import React, { Fragment } from 'react';

import Splash from '../../Containers/Splash/Splash';

const layout = (props) => {
    return (
        <Fragment>
            <p>layout</p>
            <Splash />
        </Fragment>
    )
}

export default layout;